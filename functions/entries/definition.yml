# TODO: support GET Entry by ID
---
CreateEntry:
  handler: functions/entries/handler.CreateEntryHandler
  role: EntriesLambdaRole
  environment:
    DDB_TABLE:
      Fn::ImportValue: 'EntriesTableName-${opt:stage, self:provider.stage}'
  events:
    - http:
        method: post
        path: namespace/{namespace}/entry
        cors: true
        request:
          schema:
            application/json: ${file(functions/entries/models/CreateEntry.json)}
          parameters:
            paths:
              namespace: true
